use crate::obj_loader::shader::Shader;

pub fn create_phong_shader() -> Shader {
  let mut shader = Shader::default();
  // let default_shader = shader.vertex;

  // shader.vertex = Box::new(|| {

  // });

  shader
}
